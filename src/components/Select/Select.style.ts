import styled from '@emotion/styled';

export const StyledSelect = styled.select`
  display: block;
  border: none;
  background: transparent;
  cursor: pointer;
  outline: none;
  font-size: inherit;
  color: inherit;
  margin-left: -8px;
`;
